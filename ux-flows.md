# Ollama DeskUI 0.17.0 · 全流程 UX 设计（安装 → 学习 → 深度使用 → 离开）

> 承接 `overview.md`（管理控制台 IA 收敛终版）。本文档补全的正是此前缺失的部分：**不是一张主页，而是一条完整的用户旅程**——每个功能在旅程什么位置出现、用户怎么学会它、怎么离开再回来。
> 画布页「0.17.0 · 全流程 UX」含：全生命周期 UX 地图、首次启动向导屏、对话模块屏、模型库模块屏、关键状态板（拉取进度 / 退出守门 / 托盘菜单）。

## 0. 旅程总原则（贯穿全流程）

1. **诚实层**：每一步呈现的都是本机真实状态；检测不到就如实说，不伪造、不跳过。
2. **精简收敛**：该有的有，说不清用途的一律不要——不弹网页、不注册、无常驻教学卡、无推广位。
3. **≤2 次点击**：高频操作（发送、切换会话、卸载、常驻）都一步可达。
4. **破坏性操作二次确认**：删除、退出（有任务时）必须列出后果再确认。
5. **状态持久化**：会话、配置、硬件画像落盘，任何时刻回来即续。

## 1. 阶段一 · 安装（首次接触）

目标：**装完 10 秒内见到主窗**。

| 节点 | 行为 | 界面触点 |
|---|---|---|
| deb 安装完成 | 写入系统菜单项与图标（V0.15.0 多尺寸图标已具备） | 系统菜单出现「Ollama DeskUI」 |
| 冷启动 | SSD 主窗直接打开；若有配置则进入概览，无配置则自动弹出向导 | 标题栏 + 概览驾驶舱 |
| 禁止项 | 不弹网页、不注册、不联网校验授权 | — |

## 2. 阶段二 · 首次启动（0–2 分钟）

目标：**三步知道「能不能跑、跑什么」**。向导只在首次运行出现（检测到 `~/.config/offideskollama/` 无配置时），完成后不再打扰。

三步向导（画布有第 2/3 步屏）：
1. **服务检测**：检测 Ollama——已运行（systemd / 进程，如实标注）/ 未运行（给一键启动 + 失败时列出「依次尝试了什么、为什么失败」）/ 未安装（给出发行版安装指引，不代装）。检测不可达时显示灰色点 +「未检测到」，绝不显示假绿。
2. **硬件画像**：自动识别 GPU/显存/内存（复用 `hardware.rs`），N 卡读 nvidia-smi、AMD 读 rocm-smi；读不到允许手填并持久化。右侧即时显示「可流畅 / 勉强 / 内存可跑 / 放不下」阈值说明。
3. **为你选**：按显存从精选目录（`catalog.rs`）推荐起步套餐（如 12GB → qwen2.5:7b），一键跳到模型库拉取；**可完全跳过**——「跳过向导，直接进入」常驻左下。

## 3. 阶段三 · 学习适应（首周）

目标：**以任务带学习，不加教学层**。空态即引导，用完即退。

- **模型库空态**：一个居中卡讲完三件事——「搜索你想跑的模型 → 点拉取 → 拉完直接对话」，附「或去发现中心逛逛」链接。没有分步浮层、没有功能弹窗。
- **首次拉取**：进度对话框分三阶段如实展示（清单 ✓ → 下载层 n/N → 校验），速度/剩余时间可见，「后台运行」可最小化到托盘继续（画布状态板）。
- **首条对话**：回复自带 meta 脚注（tokens · t/s · 耗时 · ctx 占用 · keep_alive），用户在真实使用中自然理解参数含义——比任何「教学页」都诚实。
- **不做的事**：无新手引导遮罩、无「每日一贴」、无广告位、无评分弹窗。

## 4. 阶段四 · 深度使用（日常高频闭环）

目标：**高频闭环 ≤2 次点击**。四个闭环（画布地图板）：

### 4.1 对话闭环（画布有模块屏）
左导航「对话」→ 会话列表（持久化）→ 选中即续聊 → 发送（Ctrl+Enter）/ 停止（Esc）→ 每条消息独立：可复制、可右键重生成 → 导出。流式中显示「正在生成…」+ 实时 token 数。
参数入口：输入区旁「参数」→ 与生成模块共用同一参数面板与预设。

### 4.2 模型闭环（画布有模块屏）
模型库：搜索 → 拉取（分阶段进度）→ 行内「详情 / 复制名 / 删除（二次确认）」；加载态在行内以绿点 +「常驻 XmYs」倒计时呈现；详情页可「加载到显存 / 卸载」。发现中心（V0.16.0）承担「不知道装什么」的场景。

### 4.3 参数闭环
预设（创意/精确/快速）→ 微调单项 → 「另存为预设」。对话与生成共用，改一处两处生效。

### 4.4 运维闭环（文档承载，画布后续可补屏）
- **运行时模块**：`/api/ps` 全景——每条加载中模型带 keep_alive 倒计时、行内「改为常驻 / 立即卸载」；未加载列表可手动预热（warming 态有明确占位，不再死等）。
- **服务模块**：运行方式（systemd/进程）如实标注 → 启停按钮 → 失败时列出诊断步骤与日志入口（复用 `server_log`）。
- **生成 / 嵌入模块**：单次生成带参数全集（system/format/think/stop/options）；嵌入面板多行输入、逐条显维度（复用现有实现）。
- **设置模块**：预设、轮询间隔、托盘行为、服务端 OLLAMA_* 配置。

## 5. 阶段五 · 离开（随时可回，画布有状态板）

目标：**关闭 ≠ 退出，状态不丢**。

| 场景 | 行为 |
|---|---|
| 关闭窗口（空闲） | 最小化到托盘；**仅首次提示一次**，之后为默认行为 |
| 退出（有任务） | 退出守门对话框：**列出将中断的任务**（拉取 60%、生成中的会话），默认推荐「最小化到托盘」，「仍要退出」需主动点击 |
| 托盘菜单 | 仅三项：打开 / 暂停服务（●服务运行中）/ 退出 |
| 回到应用 | 会话、拉取进度、硬件画像全部持久化，打开即续 |

通知策略（复用现有桌面通知）：拉取完成、模型卸载、服务错误才通知，其余静默。

## 6. 状态矩阵（全流程统一）

| 状态 | 呈现位置 | 形式 |
|---|---|---|
| 服务运行方式 | 状态栏 + 服务模块 + 托盘 | 绿点 + systemd/进程 |
| 模型加载/预热中 | 概览 · 运行时 · 对话 | 「正在加载模型…」明确占位 + 首 token 前计时 |
| keep_alive 倒计时 | 概览 · 运行时 · 模型库行内 | 「常驻 XmYs」+ 改常驻/卸载 |
| 拉取进度 | 对话框 + 托盘通知 | 三阶段 + 速度 + 后台运行 |
| 上下文占用 | 消息脚注 + 状态栏 | ctx 已用 / 上限 |
| 失败 | 原位错误行 + 服务日志 | 「依次尝试了什么、为什么失败」 |

## 7. 画布与文件索引

- Ardot 画布 `722245085909210`，页「0.17.0 · 全流程 UX」：
  - 全生命周期 UX 地图（五阶段流程板）
  - 首次启动向导屏（第 2/3 步）
  - 对话模块屏（会话列表 + 独立消息 + 输入区）
  - 模型库模块屏（工具行 + 模型行）
  - 关键状态板（拉取进度 / 退出守门 / 托盘菜单）
- 概览驾驶舱终版见同文件页「0.17.0 v2 · 管理控制台」与 `overview.md`
- 源代码未动；落地实现待拍板后另起任务
