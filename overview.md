# Ollama DeskUI 0.17.0 重设计 · 管理控制台（收敛终版）

## 0. 两条根本原则

1. **产品定位**：这是 **Ollama 在 Linux 上的全能桌面图形化管理器**，不是聊天工具。chat 只是与「模型库 / 生成 / 嵌入 / 运行时 / 服务 / 设置」平级的功能模块之一。v1 把 chat 当主界面、把模型和会话塞进同一个侧栏，心智模型反了，所以「逻辑混乱」——v2 起改为**导航驱动的模块化管理控制台**。
2. **设计语言（应用之初就强调）**：**精简、简洁、收敛**。实用、好用、丰富的同时，「该有的有，说不清用途的一律不要」。本稿每一处取舍都按这把尺子执行（见 §4 收敛决策记录）。

## 1. 约束遵守

- ✅ 功能不减：模型 / 对话 / 生成 / 嵌入 / 运行时 / 服务 / 设置全部纳入 IA。
- ✅ UI 风格样式不变：深色 Adwaita 观感保留（主底 `#2d2d30`、侧栏 `#1f1f23`、面板 `#252528`、强调蓝 `#3584e4`、状态绿 `#33d17a`、SSD 居中标题栏、圆角 6-8、Noto Sans SC）。

## 2. 信息架构（终版）

```
┌─ 标题栏 (SSD) · Ollama DeskUI · 0.17.0
├──────────────┬──────────────────────────────────────────────
│ 导航          │ 概览
│  ▣ 概览 ◀     │ ┌ 服务状态 ──┬ Ollama 版本 ┬ 已加载模型 ┬ 显存占用 ┐
│  📦 模型库    │ │ ● 运行中   │ v0.5.7      │ 1 / 3      │ 4.7/16GB │
│  💬 对话      │ │ systemd    │ API 连接正常│ 驻留显存中 │ RTX 3060 │
│  >_ 生成      │ └───────────┴────────────┴───────────┴─────────┘
│  ▤ 嵌入      │ ┌ 已加载模型 · 运行时 (1) ────────────────────────┐
│  ∿ 运行时     │ │ ● llama3.1:8b-q5_K_M   VRAM 4.7G·ctx 412/4096  │
│  🖳 服务      │ │                        [常驻 4m32s] [卸载]      │
│  ⚙ 设置      │ │ 其余 2 个未加载 · 首次调用将自动预热（warming）  │
│              │ └────────────────────────────────────────────────┘
│              │ ┌ 最近会话                          + 新建会话 ───┐
│              │ │ 💬 调试 prompt — Rust debounce   14:20·4条 [llama3.1:8b] │
│              │ │ 💬 对比 4 个模型的 JSON 输出能力  11:08·8条 [qwen2.5:7b]  │
│              │ └────────────────────────────────────────────────┘
├──────────────┴──────────────────────────────────────────────
│ ● 服务运行 (systemd) · Ollama v0.5.7        已加载 1 · VRAM 4.7GB · 模型 3 个
```

## 3. 区域详解

### 3.1 左侧导航（width 224 · `#1f1f23`）
8 个功能模块，按管理逻辑排列，选中态 `#353a44` + 白字：

| 模块 | 职责 |
|---|---|
| 概览 ◀默认 | 真实状态驾驶舱（本稿主屏） |
| 模型库 | 列表/详情/删除/复制/拉取/创建 Modelfile/模型发现 |
| 对话 | 多会话持久化聊天（每条消息独立可管理） |
| 生成 | 单次生成 + 参数全集 |
| 嵌入 | 嵌入模型 + 批量文本嵌入 |
| 运行时 | `/api/ps` 全景：keep_alive 倒计时、显存、上下文占用 |
| 服务 | daemon 状态、systemd/进程、启停、日志 |
| 设置 | 参数预设、服务端配置、托盘、轮询 |

### 3.2 概览（默认主屏）——每个元素都有明确用途
- **4 张状态卡片**：服务状态（含托管方式）/ Ollama 版本（含 API 连通性）/ 已加载（当前/总数）/ 显存占用（含 GPU 型号）。全部来自统一轮询的真实数据。
- **已加载模型 · 运行时**（全宽面板）：每行一条加载中模型——名称、VRAM、ctx 占用、「常驻 XmYs」keep_alive 倒计时（诚实层：自动卸载可预期）、「卸载」动作；底部一句 warming 提示。
- **最近会话**：最近会话快速续聊入口（名称/时间/条数/模型 tag），呼应「对话」模块。

### 3.3 状态栏（一行 · `#1a1a1d`）
左：服务运行方式 + 版本；右：已加载 · 显存 · 模型总数。一眼扫完，无第二行。

## 4. 收敛决策记录（删了什么、为什么）

| 删除项 | 理由 |
|---|---|
| 菜单栏 + 工具栏（v1） | 三段 chrome 叠加违反「精简收敛」；左侧导航已承载全部功能入口 |
| 侧栏「模型段 + 会话段」平级（v1） | 资源（模型）与活动（会话）不是同一抽象层级，混排即逻辑混乱；模型归「模型库」、会话归「对话」 |
| 本机信息面板（GPU/驱动/CPU/内存） | 不是本应用职责，系统监视器干得更好 |
| 快捷操作四按钮 | 与左侧导航 100% 重复的冗余入口 |
| 「最后刷新 14:21:03」+ 手动刷新按钮 | 统一 2 秒轮询下冗余；「任何操作完成后立即刷新」已保证实时 |
| 模块副标题「本地模型运行时驾驶舱」 | 形容性文案，违反「只描述事实与动作」 |
| 状态栏时钟 | 系统时钟已有 |

## 5. 其他模块（IA 已留位，后续模块屏另出设计稿）

- **模型库**：列表 + 详情（Modelfile/license/model_info/capabilities）+ 模型发现中心（V0.16.0 已有）。
- **对话**：会话列表 + 每条消息独立（头像/身份/正文/meta 脚注：tokens·t/s·ctx·keep_alive；流式「正在生成…」+停止）。
- **生成**：参数全集分组面板（system/format/think/stop/options）+ 输出区。
- **嵌入**：模型选择 + 批量输入 + 维度/样本预览。
- **运行时**：加载列表（keep_alive 倒计时、改常驻/卸载）+ 未加载列表（手动预热）。
- **服务**：运行方式、启停、失败诊断日志、服务端 OLLAMA_* 配置入口。
- **设置**：预设（创意/精确/快速）、轮询间隔、托盘通知。

## 6. 与评审文档对齐

| 评审 § | 主张 | 本稿落地 |
|---|---|---|
| §3.1 | 「驾驶舱 + 工作台」心智模型 | ✅ 概览即驾驶舱；工作台 = 8 个平级模块 |
| §3.2 | 菜单栏+工具栏+侧栏+状态栏 | ⚠️ 按用户「精简收敛」原则裁剪：导航替代菜单栏+工具栏；状态栏保留一行 |
| §3.3 | 诚实层：轮询、过程态、keep_alive 可见 | ✅ 概览「已加载·运行时」如实呈现 keep_alive 倒计时 / warming / ctx 占用 |
| §3.4 | 及格线 + 增值路线 | ✅ IA 为 keep_alive/num_ctx/format/think/多会话/预设全部留位 |
| §3.5 | 只描述事实与动作、无硬编码色、≤2 次点击 | ✅ 全部沿用 |

## 7. 交付物

- 设计稿 PNG（1280×820）：`Ollama DeskUI 0.17.0 · 管理控制台 概览屏（收敛终版）`
- Ardot 画布 fileId `722245085909210`，页「0.17.0 v2 · 管理控制台」（v1 旧页保留对照）
- 源代码未动；落地实现待拍板后另起任务
