# offideskOllama 设计评审与重设计方案

> 评审人视角：以「体验过该程序」的专家身份，对照产品目标逐条评价现状，再给出设计主张与实施路线。
> 产品目标（长期）：专业、易用、稳定、可全场景流程实时本地真实状态、功能全面、覆盖 ollama 原生指令能力仅为合格、UI 简单无多余设计、实事求是、UI+UX 跟随 Cinnamon。

---

## 一、总评（一句话）

现在这版是一个**功能凑齐但没成型的产品**：底子（GTK4 全异步、真实流式、托盘、真实进度）是对的，但界面像「功能清单的堆叠」而不是「一个被设计过的本地模型工作台」，而「不像 Cinnamon」的根因在技术选型（libadwaita），不是审美没跟上。

---

## 二、现状评估（对照产品目标逐条说）

### 1. 功能：及格线还没站住（对照 ollama 原生能力）

已具备：模型列表/详情/删除/复制、拉取/推送（真实进度）、Modelfile 创建、多轮流式聊天、单次生成、已加载查看与卸载、嵌入、托盘、服务启停、导出对话、生成统计。

**仍未覆盖的 ollama 原生能力（这就是「为什么感觉弱」的清单）：**

| 能力 | 现状 | 为什么用户会觉得弱 |
|---|---|---|
| `keep_alive`（模型驻留时长） | 从不设置，交给默认 5 分钟 | 模型在用户背后自动卸载 → 下一句要重新加载 → 出现「偶尔卡几秒」的假不稳定 |
| `num_ctx`（上下文窗口） | 从不显式管理 | 长对话被截断/遗忘，用户以为是模型或程序差，其实是没管上下文 |
| 上下文占用显示 | 无 | 聊到一半「模型突然忘了前面」无从诊断 |
| `format`（JSON/结构化输出） | 无 | 想拿程序化输出只能开终端 |
| 思考型模型（`think` / thinking） | 无，也不显示思考过程 | reasoning 模型等于残废 |
| `tools`（函数调用） | 无 | 完整 tool-calling 流程缺失 |
| `system`/`template`/`raw`/`suffix`/`stop` | 聊天有 system，其余无 | Modelfile 可定义的东西 GUI 摸不到 |
| options 全集（min_p/typical_p/presence/frequency/penalize_newline/draft_num_predict/num_batch/main_gpu…） | 只有 7 个常见参数 | 与 `ollama run` 的参数覆盖面仍不对等 |
| 多模态（图片输入） | 无 | 不能用视觉模型 |
| `/api/ps` 的 TTL/到期 | 有展示但无操作解释 | 「已加载」为何自己消失，无说明 |
| `ollama launch` / signin 等新命令 | 不适用（GUI 的云/集成不是本地控制台职责） | 可明确排除，避免堆功能 |

结论：**及格线（≈`ollama run/ps/stop/show` 在 GUI 里能做的事）还没到**。这是「能力弱」的客观依据。

### 2. 实时真实状态：方向对，但「全场景」只做了一半

- 3 秒轮询覆盖了在线/版本/已加载/显存 —— 好。
- 缺失：加载/卸载**过程态**（模型 warming 时界面无反馈，首 token 前的等待是死寂的）；keep_alive 倒计时不解释；上下文窗口占用；拉取分阶段进度（清单/层/校验）；服务到底以 systemd 还是进程方式运行，用户不可见。
- 轮询各自为政：侧栏列表靠动作触发刷新、已加载面板靠设置间隔、顶栏靠 3 秒——三个真相不同步。

### 3. UI/UX：不是「不够 Cinnamon」，是 libadwaita 让它不可能 Cinnamon

调研确认的事实：

- **libadwaita 应用不跟随系统 GTK 主题**（Mint-Y 等），永远呈现 GNOME/Adwaita 观感。证据：Linux Mint 22 把 libadwaita 的 GNOME 应用整体降回 GTK3 版本，理由是它们无法被 Mint 主题样式化、与 Mint 理念冲突（Linux Mint 官方论坛 + linux-console.net 报道）。
- 于是现状 = 一个「长着 GNOME 脸的 app 跑在 Cinnamon 上」，这正是「风格、思路、语言不是原生」的根因，不是缝缝补补能修的。
- Cinnamon 自家应用（Nemo/Xed/Xviewer）的范式是：**传统菜单栏 + 工具栏 + 状态栏 + 右键菜单 + 快捷键**，SSD 窗口，跟随系统主题。我们现在是 GNOME 式底栏 ViewSwitcherBar + 无菜单栏 + libadwaita 控件。

内部一致性也欠账：`ui/markdown.rs:40-92` 用 `gdk::RGBA::new(0.208,0.518,0.894,…)` 这类**硬编码 RGBA 浮点色**（链接蓝、分隔灰、错误红），不随系统主题/深浅变化，主题切换时会不协调；顶栏是裸 Box 手拼；部分文案还是「状态与动作混合」的旧腔调。

### 4. 专业/易用性的具体硬伤（使用时的痛感）

- 聊天记录是一个 TextBuffer 平铺：无法单独选中/复制某一条回答，长对话没法按消息管理。
- 换模型 = 清空会话；没有多会话、没有会话保存/恢复。专业用户做不到「对比」「回看」。
- 生成面板没有参数入口（和聊天不一致）。
- 关闭到托盘后，托盘菜单/通知对「模型卸载了」「拉取完成」无提示。
- 首次启动欢迎语是「设置说明」，不是「怎么开始用」。

---

## 三、设计主张

### 3.1 产品定位（心智模型）

把它从「四个并列工具（聊天/生成/已加载/嵌入）」重定位为：

> **本地模型运行时（Ollama）的驾驶舱 + 对话工作台。**

一句话：左边管「有什么模型、在跑什么、占多少」，中间做「聊天为主的工作」，底下一条真实状态。生成/嵌入是开发型操作，做成「按需出现」，而不是和聊天平起平坐的四个 Tab。

### 3.2 信息架构与界面（原生 Cinnamon 范式）

```
┌ 菜单栏：会话 | 模型 | 服务 | 帮助        ← 原生范式，带加速键（Ctrl+N 等）
├ 工具栏：当前模型 ▾ | 新建会话 | [服务开关] | 拉取 | 创建
├──────────────────────────────┬──────────────────────────
│ 左栏（可折叠）                 │ 主工作区
│  ▸ 模型                       │   ┌ 对话（默认）
│    [状态点] llama3.1 8B 4.7GB │   │ 消息列表（每条独立可复制）
│    [状态点] qwen3 4B 2.3GB    │   │ ├ 你 · 内容（Markdown）
│    …右键：聊天/详情/复制/删除  │   │ ├ 助手 · 内容 + meta 尾注
│  ▸ 会话（多会话持久化）         │   │ │   (128 tokens · 32 t/s · 5.2s · ctx 4k/32k)
│    （历史会话列表，回车即续）    │   │ └ （加载中… 用真实「warming」态）
│                               │   └ 输入区：文本 + 附图 + [停止][发送]
│                               │       Ctrl+Enter 发送 / Esc 停止
├──────────────────────────────┴──────────────────────────
└ 状态栏：服务●systemd | Ollama v0.x | 已加载 2/总 VRAM | 模型 5 个
```

- **不并列四个 Tab**：聊天是默认主界面；单次生成、嵌入、已加载，用「运行状态面板」或工具栏下拉收敛。
- **每个回复是一条独立消息**：可单独选择/复制，行尾右键「复制 / 重新生成 / 提问这段」；Markdown 按消息渲染。
- **菜单栏 + 加速键**：这才是 Cinnamon 的「语言」。模型操作进「模型」菜单，启停进「服务」菜单。
- **状态栏**：始终一行，讲清「服务以何种方式运行、连没连上、已加载几个、显存多少」。

### 3.3 全场景实时真实状态（诚实层）

- 统一 **RuntimeState 轮询**（约 2 秒）：`/api/ps`（含 TTL 到期倒计时）+ `/api/tags` + `/api/version`，所有面板订阅同一份真相；任何操作完成后立即触发一次刷新，不等轮询。
- **过程态必须可见**：模型加载中 = 明确的「正在加载模型…」占位（真实来自 ps 变化），不是死等；首 token 前计时；停止生成立即反馈「已中断」。
- **把隐藏的默认行为变成可见的**：keep_alive 计到哪了、还剩几秒自动卸载，在已加载列表如实显示；卸载倒计时旁给「改为常驻」。
- 所有失败出错误框/进错误行（已在做），服务启停列出「依次尝试了什么、为什么失败」。

### 3.4 能力路线图

**A. 及格线补齐（先做，对应第二节表格）：**
1. 每个请求显式传 `keep_alive`（默认较长，如 5m，用户可调），并显示/可改。
2. 上下文：聊天时估算并展示「已用 ctx / 上限」；允许设 num_ctx；超限策略告知。
3. 思考模型：显示/折叠思考过程；`think` 开关。
4. 结构化输出：回复旁一个「JSON/格式化」开关（format）。
5. 参数面板扩到 options 全集（分组收起）；system 可编辑；支持 stop 序列。
6. 图片输入（多模态），消息内可带图。
7. Modelfile 创建/详情对齐 `/api/show` 全部字段（已含 license，补 capabilities、model_info 展示）。

**B. 增值（专业，不在及格线内但提升产品力）：**
- 多会话持久化（侧栏会话列表，换模型不再丢历史）。
- 参数预设（「创意」「精确」「快速」可自定义）。
- 跨模型并排对比输出。
- 提示词/系统提示模板库。
- 服务端日志查看窗。
- 托盘通知：拉取完成/模型卸载/出错。

**C. 明确不做（避免堆砌）：**
`ollama launch`（云/外部集成）、signin/账号体系——这是「云服务客户端」的职责，不是本地控制台。要在「专业」与「简单」间守边界。

### 3.5 UI/UX 规则（写进规范）

- 用系统主题色，**禁止硬编码色**；Markdown 等需要具体色的场合从 `StyleContext` 取主题色或用语义类。
- 图标一律 Freedesktop symbolic；间距走 GTK 6/12/18 档。
- 文案：只描述「事实与动作」，不堆形容词；状态类一律实时。
- 一个操作 ≤2 次点击；破坏性操作二次确认；所有按钮可用性随真实状态走。

---

## 四、关键技术决策：去 libadwaita，回纯 GTK4

要让「UI/UX 跟随 Cinnamon」从口号变成现实，**唯一根本解是把观感载体从 libadwaita 换成跟随系统主题的纯 GTK4**：

| 现状（libadwaita） | 目标（纯 GTK4，Cinnamon 原生范式） |
|---|---|
| adw::Application | gtk4::Application |
| AdwViewStack/ViewSwitcherBar | GtkStack + GtkStackSwitcher / 场景化切换 |
| AdwMessageDialog | gtk4::AlertDialog / 普通对话框 |
| AdwPreferencesGroup | 普通 Group + 分隔（跟随系统主题） |
| AdwWindow | GtkApplicationWindow（SSD，GTK_CSD=0 保留） |
| 无菜单栏 | GtkMenuBar（会话/模型/服务/帮助 + 加速键） |

- **收益**：Mint-Y 与任意 Cinnamon 主题自动生效、深色自动跟随、一眼是「Cinnamon 的原生应用」；同时去掉 adw 依赖，安装包依赖减少。
- **代价**：UI 层结构性重构（已有逻辑/状态层不变，改动集中在控件与布局），需按阶段做、每阶段编译+冷启动验收，避免重蹈 0.11.0 覆辙。
- 若短期不迁移：至少把硬编码色清掉、控件向 GTK 标准归一、补菜单栏，能缓解观感，但「libadwaita 脸」无法根治——这要如实告诉用户取舍。

---

## 五、分阶段实施

1. **阶段 0 · 诚实层**：统一 RuntimeState 轮询/事件；补齐加载过程态、keep_alive 展示、ctx 用量；代码内清硬编码色。
2. **阶段 1 · 及格线**：keep_alive/num_ctx/format/think/参数全集/system/图片输入。
3. **阶段 2 · 架构迁移**：去 libadwaita → 纯 GTK4 + 菜单栏 + 状态栏 + 每消息 UI。
4. **阶段 3 · 增值**：多会话、预设、对比、日志窗、托盘通知。
5. 每阶段走同一验收闸：`cargo build --release` 零错误 + 单测全绿 + 真实 DISPLAY 冷启动存活 + （关键路径）用户实机确认。

---

## 六、参考来源

1. [Ollama API 官方文档（端点与参数全集）](https://github.com/ollama/ollama/blob/main/docs/api.md)
2. [Ollama CLI Reference（含较新 launch/signin 等命令）](https://docs.ollama.com/cli)
3. [Linux Mint 官方论坛：为何不支持/不用 libadwaita 应用](https://forums.linuxmint.com/viewtopic.php?t=424556)
4. [linux-console.net：Linux Mint 22 将 libadwaita 应用降回 GTK3 的报道](https://cn.linux-console.net/?p=36156)
5. [gtk-rs 书：Libadwaita 与「跟随平台 HIG」的关系](https://gtk-rs.org/gtk4-rs/stable/latest/book/libadwaita.html)
6. [GNOME 团队关于 libadwaita 只服务于 GNOME 的公开讨论（FOSS Post 综述）](https://fosspost.org/how-to-change-libadwaita-app-theme-linux/)
