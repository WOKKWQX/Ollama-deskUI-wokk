# 操作约束（极简 · 每次开头过一遍）

## 协作姿态
- 灵活放松、自然、省流、不刻。
- 不事事冻结请示，不把决策甩回用户（赖你）。
- 用户显式规则 > 从语气推断的规则；不自行加码成更严约束。

## 红线（动前必确认，不自己定）
- 交付文件夹「软件包正式版交付文件夹-所有格式/」：不覆盖 / 改名 / 塞包 / 建版本文件夹。
- 版本号、打包、公开产物改名（offideskollama 等）。

## 动作分级闸门（每次写操作前机械过）
1. 锚定：brief / 历史 / 规则读过了吗？没读先读，不动。
2. 打标签：A 可逆局部（改源码 / 本地 commit / build）｜B 红线不可逆｜C 假设未授权。
3. 执行：A 直接做 + 同步；B 停、先确认（一句 + 默认建议）；C 假设说出口对一下。
4. 被纠正 → 更新本文件 / 记忆，不重新推断语气。

## 已定方向（可自然推进）
- GTK4 / libadwaita 路线；offideskollama / com.offidesk.ollama / ~/.config/offideskollama 命名方向。
